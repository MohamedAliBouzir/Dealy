node_modules/
dist
coverage
*.js.map
*.d.ts
*.tsbuildinfo
!eslint.config.mjs
.env
.env.*
!.env.example

# Regenerated from prisma/schema.prisma at build time — never committed.
src/generated/

# Secrets / credentials — never commit real material
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
infra/nginx/certs/*
!infra/nginx/certs/.gitkeep
infra/vault/data/
infra/vault/*unseal*
*.unseal
vault-init.json

# AppRole credentials (role_id/secret_id) — mounted into the app container,
# never committed. Directory created and gitignored before either file
# exists (Phase 4 requirement, not an afterthought).
infra/vault/approle/
